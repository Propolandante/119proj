//global variables
var xBuffer = 0;
var yBuffer = 0;


//x and y must be in onMouseDown
x = event.point.x - xBuffer;
y = event.point.y - yBuffer;

//I'm not sure if it was important that
// these lines be where I put them, which
// was in the loadNextImage() function after
// raster.position.x = blah blah blah

xBuffer = raster.position.x - raster.width/2;
yBuffer = raster.position.y - raster.height/2; 